Isolation tree #0 of 200. Each path ends in a leaf holding some training sessions.
Anomalies are isolated in few steps: a short path means 'unusual'.

if api_ratio_log < 0.190:
    if unfamiliar_admin_actions < 0.067:
        if hours_outside_usual_window < 1.783:
            if travel_speed_log < 3.548:
                if unusual_weekend_login < 0.310:
                    if sensitive_ratio_log < 0.595:
                        if new_city < 0.166:
                            if travel_speed_log < 0.539:
                                leaf: 143 training session(s), path length 8
                            else:  # travel_speed_log >= 0.539
                                leaf: 7 training session(s), path length 8
                        else:  # new_city >= 0.166
                            if travel_speed_log < 0.403:
                                leaf: 2 training session(s), path length 8
                            else:  # travel_speed_log >= 0.403
                                leaf: 4 training session(s), path length 8
                    else:  # sensitive_ratio_log >= 0.595
                        if data_volume_ratio_log < 0.121:
                            leaf: 1 training session(s), path length 7
                        else:  # data_volume_ratio_log >= 0.121
                            if new_city < 0.175:
                                leaf: 3 training session(s), path length 8
                            else:  # new_city >= 0.175
                                leaf: 1 training session(s), path length 8
                else:  # unusual_weekend_login >= 0.310
                    if sensitive_ratio_log < -0.127:
                        if download_ratio_log < -0.143:
                            leaf: 1 training session(s), path length 7
                        else:  # download_ratio_log >= -0.143
                            leaf: 1 training session(s), path length 7
                    else:  # sensitive_ratio_log >= -0.127
                        leaf: 1 training session(s), path length 6
            else:  # travel_speed_log >= 3.548
                if api_ratio_log < -0.964:
                    leaf: 1 training session(s), path length 5
                else:  # api_ratio_log >= -0.964
                    if travel_speed_log < 3.957:
                        if data_volume_ratio_log < 0.263:
                            if sensitive_ratio_log < -0.246:
                                leaf: 1 training session(s), path length 8
                            else:  # sensitive_ratio_log >= -0.246
                                leaf: 3 training session(s), path length 8
                        else:  # data_volume_ratio_log >= 0.263
                            if travel_speed_log < 3.617:
                                leaf: 1 training session(s), path length 8
                            else:  # travel_speed_log >= 3.617
                                leaf: 1 training session(s), path length 8
                    else:  # travel_speed_log >= 3.957
                        if travel_speed_log < 4.116:
                            leaf: 1 training session(s), path length 7
                        else:  # travel_speed_log >= 4.116
                            leaf: 1 training session(s), path length 7
        else:  # hours_outside_usual_window >= 1.783
            if data_volume_ratio_log < -0.372:
                if data_volume_ratio_log < -1.016:
                    leaf: 1 training session(s), path length 5
                else:  # data_volume_ratio_log >= -1.016
                    if hours_outside_usual_window < 2.524:
                        if data_volume_ratio_log < -0.763:
                            leaf: 1 training session(s), path length 7
                        else:  # data_volume_ratio_log >= -0.763
                            if download_ratio_log < -0.288:
                                leaf: 1 training session(s), path length 8
                            else:  # download_ratio_log >= -0.288
                                leaf: 2 training session(s), path length 8
                    else:  # hours_outside_usual_window >= 2.524
                        leaf: 1 training session(s), path length 6
            else:  # data_volume_ratio_log >= -0.372
                if sensitive_ratio_log < -0.010:
                    if api_ratio_log < -0.014:
                        if hours_outside_usual_window < 2.903:
                            if download_ratio_log < 0.183:
                                leaf: 1 training session(s), path length 8
                            else:  # download_ratio_log >= 0.183
                                leaf: 1 training session(s), path length 8
                        else:  # hours_outside_usual_window >= 2.903
                            leaf: 1 training session(s), path length 7
                    else:  # api_ratio_log >= -0.014
                        if sensitive_ratio_log < -0.703:
                            leaf: 1 training session(s), path length 7
                        else:  # sensitive_ratio_log >= -0.703
                            leaf: 1 training session(s), path length 7
                else:  # sensitive_ratio_log >= -0.010
                    if api_ratio_log < -0.458:
                        leaf: 1 training session(s), path length 6
                    else:  # api_ratio_log >= -0.458
                        leaf: 1 training session(s), path length 6
    else:  # unfamiliar_admin_actions >= 0.067
        if api_ratio_log < -0.025:
            leaf: 1 training session(s), path length 3
        else:  # api_ratio_log >= -0.025
            if sensitive_ratio_log < -0.785:
                leaf: 1 training session(s), path length 4
            else:  # sensitive_ratio_log >= -0.785
                if data_volume_ratio_log < -0.241:
                    if sensitive_ratio_log < -0.541:
                        leaf: 1 training session(s), path length 6
                    else:  # sensitive_ratio_log >= -0.541
                        leaf: 1 training session(s), path length 6
                else:  # data_volume_ratio_log >= -0.241
                    leaf: 1 training session(s), path length 5
else:  # api_ratio_log >= 0.190
    if failed_logins < 0.640:
        if unusual_weekend_login < 0.140:
            if new_city < 0.478:
                if hours_outside_usual_window < 0.465:
                    if data_volume_ratio_log < -0.596:
                        if data_volume_ratio_log < -1.134:
                            leaf: 1 training session(s), path length 7
                        else:  # data_volume_ratio_log >= -1.134
                            if download_ratio_log < -0.539:
                                leaf: 1 training session(s), path length 8
                            else:  # download_ratio_log >= -0.539
                                leaf: 1 training session(s), path length 8
                    else:  # data_volume_ratio_log >= -0.596
                        if travel_speed_log < 1.143:
                            if api_ratio_log < 0.642:
                                leaf: 34 training session(s), path length 8
                            else:  # api_ratio_log >= 0.642
                                leaf: 1 training session(s), path length 8
                        else:  # travel_speed_log >= 1.143
                            if api_ratio_log < 0.361:
                                leaf: 2 training session(s), path length 8
                            else:  # api_ratio_log >= 0.361
                                leaf: 1 training session(s), path length 8
                else:  # hours_outside_usual_window >= 0.465
                    if sensitive_ratio_log < -0.123:
                        if data_volume_ratio_log < 0.146:
                            if data_volume_ratio_log < -0.210:
                                leaf: 1 training session(s), path length 8
                            else:  # data_volume_ratio_log >= -0.210
                                leaf: 1 training session(s), path length 8
                        else:  # data_volume_ratio_log >= 0.146
                            leaf: 1 training session(s), path length 7
                    else:  # sensitive_ratio_log >= -0.123
                        if download_ratio_log < 0.170:
                            if sensitive_ratio_log < 0.550:
                                leaf: 2 training session(s), path length 8
                            else:  # sensitive_ratio_log >= 0.550
                                leaf: 1 training session(s), path length 8
                        else:  # download_ratio_log >= 0.170
                            if hours_outside_usual_window < 1.735:
                                leaf: 4 training session(s), path length 8
                            else:  # hours_outside_usual_window >= 1.735
                                leaf: 2 training session(s), path length 8
            else:  # new_city >= 0.478
                if travel_speed_log < 0.720:
                    leaf: 1 training session(s), path length 5
                else:  # travel_speed_log >= 0.720
                    if data_volume_ratio_log < 0.360:
                        if api_ratio_log < 0.199:
                            leaf: 1 training session(s), path length 7
                        else:  # api_ratio_log >= 0.199
                            leaf: 1 training session(s), path length 7
                    else:  # data_volume_ratio_log >= 0.360
                        leaf: 1 training session(s), path length 6
        else:  # unusual_weekend_login >= 0.140
            leaf: 1 training session(s), path length 3
    else:  # failed_logins >= 0.640
        if sensitive_ratio_log < -0.969:
            leaf: 1 training session(s), path length 3
        else:  # sensitive_ratio_log >= -0.969
            if data_volume_ratio_log < 0.308:
                if data_volume_ratio_log < 0.153:
                    if new_city < 0.137:
                        if sensitive_ratio_log < 0.585:
                            leaf: 1 training session(s), path length 7
                        else:  # sensitive_ratio_log >= 0.585
                            leaf: 1 training session(s), path length 7
                    else:  # new_city >= 0.137
                        leaf: 1 training session(s), path length 6
                else:  # data_volume_ratio_log >= 0.153
                    leaf: 1 training session(s), path length 5
            else:  # data_volume_ratio_log >= 0.308
                if sensitive_ratio_log < 0.122:
                    leaf: 1 training session(s), path length 5
                else:  # sensitive_ratio_log >= 0.122
                    leaf: 1 training session(s), path length 5
