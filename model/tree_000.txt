Isolation tree #0 of 200. Each path ends in a leaf holding some training sessions.
Anomalies are isolated in few steps: a short path means 'unusual'.

if api_ratio_log < 0.190:
    if unfamiliar_admin_actions < 0.067:
        if hours_outside_usual_window < 2.378:
            if travel_speed_log < 3.548:
                if unusual_weekend_login < 0.310:
                    if sensitive_ratio_log < 0.595:
                        if new_city < 0.166:
                            if travel_speed_log < 0.539:
                                leaf: 152 training session(s), path length 8
                            else:  # travel_speed_log >= 0.539
                                leaf: 7 training session(s), path length 8
                        else:  # new_city >= 0.166
                            if travel_speed_log < 0.403:
                                leaf: 2 training session(s), path length 8
                            else:  # travel_speed_log >= 0.403
                                leaf: 4 training session(s), path length 8
                    else:  # sensitive_ratio_log >= 0.595
                        if data_volume_ratio_log < 0.121:
                            leaf: 1 training session(s), path length 7
                        else:  # data_volume_ratio_log >= 0.121
                            if new_city < 0.175:
                                leaf: 3 training session(s), path length 8
                            else:  # new_city >= 0.175
                                leaf: 1 training session(s), path length 8
                else:  # unusual_weekend_login >= 0.310
                    if sensitive_ratio_log < -0.127:
                        if download_ratio_log < -0.143:
                            leaf: 1 training session(s), path length 7
                        else:  # download_ratio_log >= -0.143
                            leaf: 1 training session(s), path length 7
                    else:  # sensitive_ratio_log >= -0.127
                        leaf: 1 training session(s), path length 6
            else:  # travel_speed_log >= 3.548
                if api_ratio_log < -0.964:
                    leaf: 1 training session(s), path length 5
                else:  # api_ratio_log >= -0.964
                    if travel_speed_log < 3.957:
                        if data_volume_ratio_log < 0.263:
                            if sensitive_ratio_log < -0.246:
                                leaf: 1 training session(s), path length 8
                            else:  # sensitive_ratio_log >= -0.246
                                leaf: 3 training session(s), path length 8
                        else:  # data_volume_ratio_log >= 0.263
                            if travel_speed_log < 3.617:
                                leaf: 1 training session(s), path length 8
                            else:  # travel_speed_log >= 3.617
                                leaf: 1 training session(s), path length 8
                    else:  # travel_speed_log >= 3.957
                        if travel_speed_log < 4.116:
                            leaf: 1 training session(s), path length 7
                        else:  # travel_speed_log >= 4.116
                            leaf: 1 training session(s), path length 7
        else:  # hours_outside_usual_window >= 2.378
            if api_ratio_log < -0.373:
                if api_ratio_log < -0.467:
                    leaf: 1 training session(s), path length 5
                else:  # api_ratio_log >= -0.467
                    leaf: 1 training session(s), path length 5
            else:  # api_ratio_log >= -0.373
                if hours_outside_usual_window < 3.524:
                    leaf: 1 training session(s), path length 5
                else:  # hours_outside_usual_window >= 3.524
                    leaf: 1 training session(s), path length 5
    else:  # unfamiliar_admin_actions >= 0.067
        if download_ratio_log < -0.456:
            leaf: 1 training session(s), path length 3
        else:  # download_ratio_log >= -0.456
            if hours_outside_usual_window < 0.052:
                if sensitive_ratio_log < -0.459:
                    leaf: 1 training session(s), path length 5
                else:  # sensitive_ratio_log >= -0.459
                    leaf: 1 training session(s), path length 5
            else:  # hours_outside_usual_window >= 0.052
                if api_ratio_log < 0.090:
                    leaf: 1 training session(s), path length 5
                else:  # api_ratio_log >= 0.090
                    leaf: 1 training session(s), path length 5
else:  # api_ratio_log >= 0.190
    if hours_outside_usual_window < 1.805:
        if new_city < 0.802:
            if sensitive_ratio_log < 0.492:
                if data_volume_ratio_log < 0.234:
                    if data_volume_ratio_log < -1.152:
                        leaf: 1 training session(s), path length 6
                    else:  # data_volume_ratio_log >= -1.152
                        if api_ratio_log < 0.428:
                            if download_ratio_log < 0.191:
                                leaf: 16 training session(s), path length 8
                            else:  # download_ratio_log >= 0.191
                                leaf: 8 training session(s), path length 8
                        else:  # api_ratio_log >= 0.428
                            if sensitive_ratio_log < -0.603:
                                leaf: 1 training session(s), path length 8
                            else:  # sensitive_ratio_log >= -0.603
                                leaf: 7 training session(s), path length 8
                else:  # data_volume_ratio_log >= 0.234
                    if download_ratio_log < 0.218:
                        if download_ratio_log < 0.007:
                            if data_volume_ratio_log < 0.353:
                                leaf: 1 training session(s), path length 8
                            else:  # data_volume_ratio_log >= 0.353
                                leaf: 1 training session(s), path length 8
                        else:  # download_ratio_log >= 0.007
                            if unusual_weekend_login < 0.233:
                                leaf: 5 training session(s), path length 8
                            else:  # unusual_weekend_login >= 0.233
                                leaf: 1 training session(s), path length 8
                    else:  # download_ratio_log >= 0.218
                        if download_ratio_log < 0.283:
                            leaf: 1 training session(s), path length 7
                        else:  # download_ratio_log >= 0.283
                            if data_volume_ratio_log < 0.572:
                                leaf: 2 training session(s), path length 8
                            else:  # data_volume_ratio_log >= 0.572
                                leaf: 3 training session(s), path length 8
            else:  # sensitive_ratio_log >= 0.492
                if download_ratio_log < -0.035:
                    if failed_logins < 0.297:
                        if api_ratio_log < 0.426:
                            leaf: 1 training session(s), path length 7
                        else:  # api_ratio_log >= 0.426
                            leaf: 1 training session(s), path length 7
                    else:  # failed_logins >= 0.297
                        leaf: 1 training session(s), path length 6
                else:  # download_ratio_log >= -0.035
                    if api_ratio_log < 0.431:
                        if sensitive_ratio_log < 0.669:
                            if data_volume_ratio_log < 0.438:
                                leaf: 2 training session(s), path length 8
                            else:  # data_volume_ratio_log >= 0.438
                                leaf: 1 training session(s), path length 8
                        else:  # sensitive_ratio_log >= 0.669
                            if data_volume_ratio_log < 0.632:
                                leaf: 2 training session(s), path length 8
                            else:  # data_volume_ratio_log >= 0.632
                                leaf: 1 training session(s), path length 8
                    else:  # api_ratio_log >= 0.431
                        if download_ratio_log < 0.259:
                            leaf: 1 training session(s), path length 7
                        else:  # download_ratio_log >= 0.259
                            leaf: 1 training session(s), path length 7
        else:  # new_city >= 0.802
            if api_ratio_log < 0.302:
                if hours_outside_usual_window < 0.735:
                    if failed_logins < 0.202:
                        if data_volume_ratio_log < 0.200:
                            leaf: 1 training session(s), path length 7
                        else:  # data_volume_ratio_log >= 0.200
                            leaf: 1 training session(s), path length 7
                    else:  # failed_logins >= 0.202
                        leaf: 1 training session(s), path length 6
                else:  # hours_outside_usual_window >= 0.735
                    leaf: 1 training session(s), path length 5
            else:  # api_ratio_log >= 0.302
                leaf: 1 training session(s), path length 4
    else:  # hours_outside_usual_window >= 1.805
        if api_ratio_log < 0.322:
            leaf: 1 training session(s), path length 3
        else:  # api_ratio_log >= 0.322
            leaf: 1 training session(s), path length 3
